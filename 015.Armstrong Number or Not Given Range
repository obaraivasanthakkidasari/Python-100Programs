start = 1
end = 1000

for number in range(start, end + 1):
    num = number
    total = 0
    length = len(str(num))

    while num > 0:
        digit = num % 10
        num = num // 10
        total += pow(digit, length)

    if total == number:
        print(number, "Armstrong")
