start = 1
end = 100

for num in range(start, end + 1):
    if num < 2:
        print(num, "Not Prime")
        continue

    flag = 0
    for i in range(2, num):
        if num % i == 0:
            flag = 1
            break

    if flag == 1:
        print(num, "Not Prime")
    else:
        print(num, "Prime")
