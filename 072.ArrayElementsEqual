def can_all_be_equal(arr):
    return len(set(arr)) == 1

# Example
arr = [5, 5, 5, 5]
if can_all_be_equal(arr):
    print("Yes, all numbers are equal")
else:
    print("No, all numbers are not equal")
